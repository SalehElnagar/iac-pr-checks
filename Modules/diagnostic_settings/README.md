# Diagnostic Settings Module

Creates a single diagnostic setting targeting Log Analytics.

## Notes

- The module is intentionally focused on Log Analytics because that is the Article 1 requirement.
- Use `enabled_logs` with `category_group = "allLogs"` for a concise baseline configuration.
